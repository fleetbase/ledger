<Layout::Resource::Panel
    @title={{@model.name}}
    @actionButtons={{this.actionButtons}}
    @onClose={{route-action "transitionTo" "console.ledger.accounting.accounts.index"}}
>
    <:header>
        <Account::PanelHeader @account={{@model}} />
    </:header>
    <:default>
        <TabNavigation @tabs={{this.tabs}}>
            <:default>
                {{outlet}}
            </:default>
        </TabNavigation>
    </:default>
</Layout::Resource::Panel>
